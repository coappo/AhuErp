<UserControl x:Class="AhuErp.UI.Views.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:JournalViewModel}"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Журналы регистрации"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Border Grid.Row="1" Padding="12" Background="White" CornerRadius="4" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Журнал:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding AvailableKinds}"
                          SelectedItem="{Binding SelectedKind}"
                          ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                          MinWidth="180" Margin="0,0,12,0" Padding="6" />
                <TextBlock Text="Дело:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding Cases}"
                          SelectedItem="{Binding SelectedCase}"
                          DisplayMemberPath="Title"
                          MinWidth="220" Margin="0,0,12,0" Padding="6" />
                <TextBlock Text="С:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <DatePicker SelectedDate="{Binding FromDate}" Width="120" Margin="0,0,12,0" />
                <TextBlock Text="По:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <DatePicker SelectedDate="{Binding ToDate}" Width="120" Margin="0,0,12,0" />
                <CheckBox Content="Только просроченные"
                          IsChecked="{Binding OverdueOnly}"
                          VerticalAlignment="Center" Margin="0,0,12,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Padding="12" Background="White" CornerRadius="4" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Текст:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <TextBox Text="{Binding TextFilter, UpdateSourceTrigger=PropertyChanged}"
                         Width="220" Padding="6" Margin="0,0,12,0" />
                <TextBlock Text="Корреспондент:" Margin="0,0,4,0" VerticalAlignment="Center" />
                <TextBox Text="{Binding CorrespondentFilter, UpdateSourceTrigger=PropertyChanged}"
                         Width="220" Padding="6" Margin="0,0,12,0" />
                <Button Content="Применить" Command="{Binding ApplyFilterCommand}" Padding="10,4" Margin="0,0,6,0" />
                <Button Content="Экспорт в Excel" Command="{Binding ExportCommand}" Padding="10,4" />
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Рег. №" Binding="{Binding RegistrationNumber}" Width="120" />
                <DataGridTextColumn Header="Дата" Binding="{Binding RegistrationDate, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Корреспондент" Binding="{Binding Correspondent}" Width="180" />
                <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Status, Converter={StaticResource EnumDisplayConverter}}" Width="120" />
                <DataGridTextColumn Header="Исполнитель" Binding="{Binding AssignedEmployee.FullName}" Width="180" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#0F766E" Margin="0,0,12,0" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#B91C1C" />
        </StackPanel>
    </Grid>
</UserControl>
