<UserControl x:Class="AhuErp.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReportsViewModel}"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Регламентированные отчёты"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Border Grid.Row="1" Padding="12" Background="White" CornerRadius="4" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker SelectedDate="{Binding FromDate}" Margin="0,0,8,0" />
                <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker SelectedDate="{Binding ToDate}" Margin="0,0,16,0" />
                <TextBlock Text="Дело:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox ItemsSource="{Binding Cases}" SelectedItem="{Binding SelectedCase}"
                          DisplayMemberPath="Index" MinWidth="120" Margin="0,0,16,0" />
                <TextBlock Text="Документ:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}"
                          DisplayMemberPath="RegistrationNumber" MinWidth="160" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock Text="Документооборот" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Button Content="Реестр исходящих за период (XLSX)"
                        Command="{Binding ExportOutgoingDispatchCommand}"
                        HorizontalAlignment="Stretch" Padding="10,6" Margin="0,0,0,6" />
                <Button Content="Опись дела (DOCX)"
                        Command="{Binding ExportCaseInventoryCommand}"
                        HorizontalAlignment="Stretch" Padding="10,6" Margin="0,0,0,6" />
                <Button Content="История документа (PDF)"
                        Command="{Binding ExportAuditTrailCommand}"
                        HorizontalAlignment="Stretch" Padding="10,6" Margin="0,0,0,6" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Хозяйственная деятельность" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Button Content="Отчёт по парку (XLSX)"
                        Command="{Binding ExportFleetCommand}"
                        HorizontalAlignment="Stretch" Padding="10,6" Margin="0,0,0,6" />
                <Button Content="Оборот склада (XLSX)"
                        Command="{Binding ExportInventoryTurnoverCommand}"
                        HorizontalAlignment="Stretch" Padding="10,6" Margin="0,0,0,6" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Crimson" />
        </StackPanel>
    </Grid>
</UserControl>
